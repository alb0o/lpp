// Test pipeline operator
fn double(x: int) -> int {
    return x * 2;
}

fn triple(x: int) -> int {
    return x * 3;
}

fn main() -> int {
    let x = 5;
    let result = x |> double |> triple;
    print(result);
    return 0;
}
